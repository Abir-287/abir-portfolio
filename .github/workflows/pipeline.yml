name: Portfolio CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env:
      CI: true
      NEXT_PUBLIC_GIT_SHA: ${{ github.sha }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run linting (strict mode)
        run: npx next lint --strict

      - name: Build project
        run: npm run build

      - name: Run security audit
        run: npm audit --audit-level moderate

      - name: Run basic validation
        run: |

          # node -e "console.log('✅ Basic JavaScript validation passed')"

      - name: Run tests
        run: npm test -- --watchAll=false --coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy to Server
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/portfolio
            timeout 300 ./deploy.sh || exit 1

      - name: Health check
        run: |
          sleep 5
          curl -f http://${{ secrets.SERVER_IP }} || exit 1

      - name: Success notification
        run: echo "✅ Deployment completed at $(date)"
